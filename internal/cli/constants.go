package cli

// Shared flag name constants used across multiple commands.
const (
	flagFormat    = "format"
	flagOutputDir = "output-dir"
	flagType      = "type"
	flagWorkers   = "workers"

	defaultFormat = "json"
)
